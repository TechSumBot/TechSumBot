"static final is seen often because it means application1-wide constant."static and final are entirely different things."However, sometimes you'll see a non-static final field when an object has a immutable property."Final fields do not need to be static, and sometimes it can be useful to have a non-static final instance variable."final means that the field can't change value after initial assignment (which must occur during type/instance initialization).