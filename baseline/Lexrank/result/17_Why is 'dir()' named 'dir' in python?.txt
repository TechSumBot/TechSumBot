"DIR does directory listings, like the Unix command ls."[dir(): Display a namespace's names (hyper-link)]"list() is used for creating a lists, so dir() is used for listing elements object which has a similar tree-like structure to file system.dir(__builtins__) outputs a view of 'Python's builtin world'.This is not a directory as used in file systems, but the standard usage: a listing of names or data.